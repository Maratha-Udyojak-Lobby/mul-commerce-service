# MUL Commerce Service

Product catalog, inventory management, shopping cart, and orders.

## Development

```bash
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 8004
```

Server runs on http://127.0.0.1:8004
